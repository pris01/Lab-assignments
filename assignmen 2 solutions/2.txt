#include <stdio.h>

void main()
{
    int n,i=1;

    printf("Enter the number to draw its table = ");
    scanf("%d",&n);

    while(i<=10)
    {
        printf("\n %d x %d = %d", n,i,n*i);
        i++;
    }

}